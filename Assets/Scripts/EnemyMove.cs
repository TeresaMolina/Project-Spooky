using UnityEngine;

public class EnemyMove : MonoBehaviour
{

    //if the enemy touches the player the player dies (gets a jump scare scene) and will return to the title screen.
    //if the flashlight reaches the enemy the enemy will be stopped in their place.

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
